#include <iostream>
#include "Sales_item.h"

int main() {
  Sales_item item;
  int i = 0;
  if (std::cin >> item) {
    Sales_item temp;
    temp.isbn() = item.isbn();
    i += 1;
    while (std::cin >> item) {
      if
    }
  }

  return 0;
}
